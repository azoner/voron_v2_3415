# =====================================================================
# Voron V2 #3415 — printer.cfg
# ---------------------------------------------------------------------
# Placeholder. Replace with the actual Klipper configuration as it
# stabilizes. This file is the single source of truth — it is embedded
# verbatim into content/config/printer.md via the include-code shortcode.
#
# After editing on the printer host:
#   - RESTART          : reload config without resetting the MCU
#   - FIRMWARE_RESTART : also reset the MCU (use after pin changes)
#   - SAVE_CONFIG      : Klipper writes the auto-generated block below
#                        the #*# <---- SAVE_CONFIG ----> marker.
# Never hand-edit anything below that marker.
# =====================================================================

# [mcu]
# serial: /dev/serial/by-id/...

# [printer]
# kinematics: corexy
# max_velocity: 300
# max_accel: 3000
# max_z_velocity: 15
# max_z_accel: 350
# square_corner_velocity: 5.0
